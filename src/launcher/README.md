# Directory: /src/launcher

User-end Launcher (MCA library wrapper) for interacting with the end user.
Provides project bootstrap & requirements check, user-oriented configuration, wide error-handling (with user-oriented messages),
incremental version downloading, etc.
