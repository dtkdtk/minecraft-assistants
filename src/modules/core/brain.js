import * as mf from "mineflayer";
import EventEmitter from "node:events";

export default class Mod_Brain extends EventEmitter {

  /**
   * @param {Bot} bot 
   */
  constructor(bot) {
    super();
  }

}
